apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-a4
  labels:
    grafana_dashboard: "1"
data:
  a4-decision-dashboard.json: |-
{{ .Files.Get "dashboards/a4-decision-dashboard.json" | nindent 4 }}
