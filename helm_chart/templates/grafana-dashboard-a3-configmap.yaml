apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-grafana-a3
  labels:
    grafana_dashboard: "1"
data:
  a3-dashboard.json: |-
{{ .Files.Get "dashboards/a3-dashboard.json" | nindent 4 }}
